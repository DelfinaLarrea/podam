general:
  build_dir: api
  
test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex "podam/src/test/resources/target/surefire-reports.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
